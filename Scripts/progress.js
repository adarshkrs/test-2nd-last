// Write code for the Progress page here 

let navbar = document.createElement('navbar')
  navbar=JSON.parse(document.getElementById('navbar',dash))

  form.addEventListener("submit",function(e){
    e.preventDefault();
     navbarobj={
        
     }
  })